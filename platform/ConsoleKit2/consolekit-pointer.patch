--- ConsoleKit-0.4.6/tools/ck-history.c	2013-07-19 04:15:28.000000000 +0000
+++ ConsoleKit-0.4.6-nnew/tools/ck-history.c	2026-03-12 02:52:55.068808073 +0000
@@ -85,7 +85,7 @@
 
         events = NULL;
         hit_since = FALSE;
-        while (gzgets (fstream, line, sizeof (line)) != Z_NULL) {
+        while (gzgets (*fstream, line, sizeof (line)) != Z_NULL) {
                 CkLogEvent *event;
 
                 if (strlen (line) == sizeof (line) - 1) {
@@ -150,7 +150,7 @@
         gboolean ret;
 
         if (g_str_has_suffix (filename, ".gz")) {
-                gzFile *f;
+                gzFile f;
                 f = gzopen (filename, "r");
                 if (f == NULL) {
                         int         errnum;
@@ -164,7 +164,7 @@
                                    errmsg);
                         return FALSE;
                 }
-                ret = process_log_gzstream (f, since);
+                ret = process_log_gzstream (&f, since);
                 gzclose (f);
         } else {
                 FILE    *f;
