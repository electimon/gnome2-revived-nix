--- gdm-2.30.7/configure	2011-06-02 11:14:58.000000000 -0400
+++ gdm-2.30.7-new/configure	2026-06-17 00:00:08.178379785 -0400
@@ -1,4 +1,4 @@
-#! /bin/sh
+#!/nix/store/6ph0zypyfc09fw6hlc1ygjvk2hv4j9vd-bash-5.3p3/bin/sh
 # Guess values for system-dependent variables and create Makefiles.
 # Generated by GNU Autoconf 2.63 for gdm 2.30.7.
 #
@@ -838,6 +838,7 @@
 authdir
 RBAC_LIBS
 DBUS_SYS_DIR
+DBUS_DAEMON_DIR
 WITH_CONSOLE_KIT
 WITH_CONSOLE_KIT_FALSE
 WITH_CONSOLE_KIT_TRUE
@@ -1104,6 +1105,7 @@
 with_group
 with_pam_prefix
 with_dbus_sys
+with_dbus_daemon_dir
 with_dbus_services
 enable_rbac_shutdown
 with_log_dir
@@ -35187,6 +35189,10 @@
   withval=$with_dbus_sys;
 fi
 
+if test "${with_dbus_daemon_dir+set}" = set; then
+  withval=$with_dbus_daemon_dir;
+fi
+
 
 # Check whether --with-dbus-services was given.
 if test "${with_dbus_services+set}" = set; then
@@ -35200,6 +35206,12 @@
         DBUS_SYS_DIR="$sysconfdir/dbus-1/system.d"
 fi
 
+if ! test -z "$with_dbus_daemon_dir" ; then
+        DBUS_DAEMON_DIR="$with_dbus_daemon_dir"
+else
+        DBUS_DAEMON_DIR="$sysconfdir/dbus-1/system.d"
+fi
+
 
 
 { $as_echo "$as_me:$LINENO: checking whether iso-codes has iso-639 domain" >&5
